
2025-12-18T12:23:56.948659
================================ System Message ================================

You will get instructions for code to write.
You will write a very long answer. Make sure that every detail of the architecture is, in the end, implemented as code.
Think step by step and reason yourself to the correct decisions to make sure we get it right.
First lay out the names of the core classes, functions, methods that will be necessary, As well as a quick comment on their purpose.

You will output the content of each file necessary to achieve the goal, including ALL code.
Represent files like so:

FILENAME
```
CODE
```

The following tokens must be replaced like so:
FILENAME is the lowercase combined path and file name including the file extension
CODE is the code in the file

Example representation of a file:

src/hello_world.py
```
print("Hello World")
```

Do not comment on what every file does. Please note that the code should be fully functional. No placeholders.


You will start with the "entrypoint" file, then go to the ones that are imported by that file, and so on.
Please note that the code should be fully functional. No placeholders.

Follow a language and framework appropriate best practice file naming convention.
Make sure that files contain all imports, types etc.  The code should be fully functional. Make sure that code in different files are compatible with each other.
Ensure to implement all code, if you are unsure, write a plausible implementation.
Include module dependency or package manager dependency definition file.
Before you finish, double check that all parts of the architecture is present in the files.

When you are done, write finish with "this concludes a fully working implementation".

Useful to know:
Almost always put different classes in different files.
Always use the programming language the user asks for.
For Python, you always create an appropriate requirements.txt file.
For NodeJS, you always create an appropriate package.json file.
Always add a comment briefly describing the purpose of the function definition.
Add comments explaining very complex bits of logic.
Always follow the best practices for the requested languages for folder/file structure and how to package the project.


Python toolbelt preferences:
- pytest
- dataclasses


================================ Human Message =================================

Request: Create a small web application using Node.js with ExpressJS and plain JavaScript that does the following:

Reads a configurable local folder containing multiple PDF and EPUB ebooks.

On each page load (or browser refresh), selects:

a random ebook from the folder, and

a random page from that ebook.

Renders that selected page directly in the browser.

Additional requirements:

The app should automatically detect whether a file is PDF or EPUB and handle each format appropriately.

No manual user selection: the book and page must be chosen randomly on every refresh.

There should be a label displaying the filename of the book and the number of the page.

The server should expose a single route (e.g. /) that performs the random selection and serves the content.

Include clear instructions for installing dependencies and running the app locally.

Prefer lightweight, well-maintained libraries for PDF and EPUB rendering.

The final result should be a minimal but functional ExpressJS app that runs locally and works by simply refreshing the browser.

================================== Ai Message ==================================

To create a small web application using Node.js with ExpressJS that meets the specified requirements, we will need to implement several components. Here's a breakdown of the core classes, functions, and methods that will be necessary:

1. **app.js**: The main entry point of the application. It will set up the Express server and define the route for serving the random ebook page.

2. **ebookService.js**: A service to handle the logic of selecting a random ebook and a random page. It will also determine the file type (PDF or EPUB) and use appropriate libraries to extract and render the content.

3. **pdfRenderer.js**: A utility to handle rendering of PDF files. It will use a library to extract text from a random page of a PDF.

4. **epubRenderer.js**: A utility to handle rendering of EPUB files. It will use a library to extract text from a random page of an EPUB.

5. **public/index.html**: A simple HTML file to display the ebook content in the browser.

6. **package.json**: The package manager dependency definition file.

7. **README.md**: Instructions for installing dependencies and running the app locally.

Let's implement the code for each of these components.

**app.js**
```javascript
const express = require('express');
const path = require('path');
const ebookService = require('./services/ebookService');

const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', async (req, res) => {
    try {
        const { content, filename, pageNumber } = await ebookService.getRandomEbookPage();
        res.send(`
            <html>
                <head><title>Random Ebook Page</title></head>
                <body>
                    <h1>${filename} - Page ${pageNumber}</h1>
                    <div>${content}</div>
                </body>
            </html>
        `);
    } catch (error) {
        res.status(500).send('Error loading ebook page');
    }
});

app.listen(PORT, () => {
    console.log(`Server is running on http://localhost:${PORT}`);
});
```

**services/ebookService.js**
```javascript
const fs = require('fs');
const path = require('path');
const pdfRenderer = require('../utils/pdfRenderer');
const epubRenderer = require('../utils/epubRenderer');

const EBOOKS_DIR = path.join(__dirname, '../ebooks');

async function getRandomEbookPage() {
    const files = fs.readdirSync(EBOOKS_DIR);
    const ebookFile = files[Math.floor(Math.random() * files.length)];
    const filePath = path.join(EBOOKS_DIR, ebookFile);

    if (ebookFile.endsWith('.pdf')) {
        return await pdfRenderer.renderRandomPage(filePath);
    } else if (ebookFile.endsWith('.epub')) {
        return await epubRenderer.renderRandomPage(filePath);
    } else {
        throw new Error('Unsupported file format');
    }
}

module.exports = {
    getRandomEbookPage
};
```

**utils/pdfRenderer.js**
```javascript
const fs = require('fs');
const pdfParse = require('pdf-parse');

async function renderRandomPage(filePath) {
    const dataBuffer = fs.readFileSync(filePath);
    const data = await pdfParse(dataBuffer);
    const numPages = data.numpages;
    const randomPageNumber = Math.floor(Math.random() * numPages) + 1;
    const pageContent = data.text.split('\n\n')[randomPageNumber - 1];

    return {
        content: pageContent || 'No content available',
        filename: filePath.split('/').pop(),
        pageNumber: randomPageNumber
    };
}

module.exports = {
    renderRandomPage
};
```

**utils/epubRenderer.js**
```javascript
const EPub = require('epub');

function renderRandomPage(filePath) {
    return new Promise((resolve, reject) => {
        const epub = new EPub(filePath);

        epub.on('end', () => {
            const numChapters = epub.flow.length;
            const randomChapterIndex = Math.floor(Math.random() * numChapters);
            const chapter = epub.flow[randomChapterIndex];

            epub.getChapter(chapter.id, (error, text) => {
                if (error) {
                    reject(error);
                } else {
                    resolve({
                        content: text || 'No content available',
                        filename: filePath.split('/').pop(),
                        pageNumber: randomChapterIndex + 1
                    });
                }
            });
        });

        epub.on('error', reject);
        epub.parse();
    });
}

module.exports = {
    renderRandomPage
};
```

**public/index.html**
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random Ebook Page</title>
</head>
<body>
    <h1>Random Ebook Page</h1>
    <div id="content"></div>
</body>
</html>
```

**package.json**
```json
{
  "name": "random-ebook-viewer",
  "version": "1.0.0",
  "description": "A simple web app to display random pages from ebooks",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "epub": "^0.3.0",
    "express": "^4.18.2",
    "pdf-parse": "^1.1.1"
  }
}
```

**README.md**
```
# Random Ebook Viewer

This is a simple Node.js application using ExpressJS to display random pages from PDF and EPUB ebooks.

## Installation

1. Clone the repository:
   ```
   git clone <repository-url>
   ```

2. Navigate to the project directory:
   ```
   cd random-ebook-viewer
   ```

3. Install the dependencies:
   ```
   npm install
   ```

4. Place your PDF and EPUB files in the `ebooks` directory.

## Running the Application

1. Start the server:
   ```
   npm start
   ```

2. Open your browser and go to `http://localhost:3000` to view a random ebook page.

## Dependencies

- ExpressJS
- pdf-parse
- epub
```

This concludes a fully working implementation.
